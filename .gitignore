build-*/
build/
*.o
*.a
*.elf
*.bin
!make-cd/tools/IP.BIN
# make-cd/tools/mkwad/common-key.bin is the user-provided Wii common key
# consumed by mkwad; already matched by *.bin above but called out so it
# isn't accidentally whitelisted later.
*.cdi
*.dol
*.lzo
.DS_Store
include/kosload/version.h
client/playstation2/iop/dev9_init_bin.h
make-cd/tools/elf2dol
.github/
.claude/

# External / vendored research material — not part of the build, do not commit
AGENT/
